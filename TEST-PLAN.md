# Test Coverage Plan

## A note on "100% coverage" here

OpenEMR is a third-party system under test, not your own codebase, so line/branch coverage tools (coverlet, istanbul) don't apply the way they would on a project like Shenny or treeLine. "100% coverage" in this repo means 100% of the scenario matrix below — every resource, table, and workflow gets at least a happy path, a negative/edge case, and (where relevant) a boundary/authorization case. That's the metric to track, and it's also the stronger interview story: it shows you scope coverage deliberately instead of chasing a percentage a tool spits out.

Status legend: `[x]` scaffolded with a real test, `[ ]` planned, not yet written.

## Layer 1 — API (C#/xUnit)

### Legacy REST (`/apis/{site}/api`)
- [x] Patient: create, list, get by id, update, missing-required-field
- [x] Appointment: create, list-by-patient, double-booking (confirmed allowed as a soft warning, not a hard conflict)
- [x] Appointment: cancel (confirmed a real hard delete via `AppointmentService::deleteAppointmentRecord()`, distinct from the UI's still-broken cancel flow); update/reschedule and recurring series are both confirmed **not supported by this REST API at all** — there is no `PUT`/`PATCH` route or controller method for this resource (confirmed 404), and `AppointmentService::insert()` only reads a fixed set of fields and silently ignores any recurrence-shaped fields in the POST body rather than creating a real series
- [x] Encounter: create, list-by-patient, close/sign encounter
- [x] Practitioner: list, get by id
- [x] Facility: create, get by id, update, list, missing-required-field, invalid-uuid
- [x] Insurance Company: list, create (confirmed 500 — broken in this OpenEMR version), update (same bug)
- [x] Patient Insurance: create, get by id, list, update (confirmed destructive — nulls unset fields), missing-required-field, not-found
- [x] Allergy: create, missing-required-field, list-by-patient (confirmed the patient-nested list/get routes are broken — always return empty; real list-by-patient coverage uses the top-level route's `puuid` filter instead), delete
- [x] Immunization: read/search-only (`rs`, no create endpoint exists — confirmed via 404 on POST), list filtered by `patient_id` works correctly, get by uuid, 404 on nonexistent uuid
- [x] Procedure: read/search-only (`rs`, confirmed 404 on POST), list/get-by-uuid work for well-formed fixtures but the list route ignores any query-string filter entirely (no real list-by-patient is possible), and both routes 500 for any row whose `encounter_id`/`provider_id` doesn't resolve to a real row (confirmed bug, `ProcedureService`)
- [x] Prescription: read/search-only (`rs`, confirmed 404 on POST), list works but also ignores any query-string filter (no real list-by-patient), and `GET /api/prescription/{uuid}` is unconditionally broken — always 500, valid uuid or not (confirmed bug, `PrescriptionService::getOne()`)
- [x] Document: upload, list-by-patient, download, missing-file, missing-path (confirmed the `path` category resolution and the missing-required-query-param cases are both real bugs — see `CONTEXT.md`), download-nonexistent, empty-category list, no update/delete route
- [x] Message: create, missing-required-field, no read/list route (confirmed 404), update (confirmed it appends rather than replaces the body, and — a real bug — never scopes by pid so a mismatched patient id in the URL still updates another patient's note), delete (confirmed soft-delete via `deleted=1`, correctly scoped by pid+id, but — a real bug — reports the same `200` success whether or not any row actually matched, including for a nonexistent message id)

### FHIR R4 (`/apis/{site}/fhir`)
- [x] Patient search bundle — genuinely passing now (see the resolved root cause below), not just the OAuth-scope workaround previously assumed
- [x] Appointment search bundle — same fix, same result
- [x] Encounter, Condition, AllergyIntolerance, MedicationRequest, Observation search + read — all 5 scopes added to `appsettings.test.json`. Encounter/AllergyIntolerance/Condition/MedicationRequest all go further than the bare Patient/Appointment bundle-shape check: each creates real fixture data (via the existing REST create endpoint for Encounter/Allergy, via a direct SQL insert for Condition/MedicationRequest since neither has one), then asserts a `patient`-filtered search actually returns that entry — not just a technically-valid bundle, which a bare unfiltered search could return even when patient-scoped filtering is silently broken. Confirmed live: the FHIR `Patient.id` is the exact same uuid as the REST `puuid` used everywhere else in this project, so no separate id-translation was needed. Observation is the one exception — its real backing table (`form_vitals`) additionally requires a `forms` table linkage row to be visible to search at all (confirmed by reading `VitalsService::search()`, which joins to `forms`), and reproducing that live via direct SQL produced an unexplained `500` not yet root-caused; `ObservationApiTests.cs` therefore only asserts the same bare-bundle-shape pattern as Patient/Appointment for now — see `CONTEXT.md` Known Constraints for the full detail and the open follow-up
- [x] $everything operation on Patient — confirmed **not implemented at all in this OpenEMR version**: no `$everything` route exists anywhere in `_rest_routes.inc.php` (unlike the neighboring `$export` bulk-export operation, which is registered). `GET /fhir/Patient/{uuid}/$everything` returns a clean `404`, confirmed by `Fhir_Patient_Everything_Operation_Returns_NotFound_Unsupported_By_This_Version` in `PatientApiTests.cs`
- [x] Bundle transaction POST — confirmed **not implemented at all**: there is no bare `POST /fhir` route registered for accepting a transaction `Bundle`; every FHIR `POST` route in this version is per-resource (e.g. `POST /fhir/Patient`). The only routes containing the word "transaction" belong to an entirely unrelated legacy REST resource (`TransactionRestController`, financial/billing transactions, `/api/patient/{pid}/transaction`) — confirmed by reading source before writing the assertion, not guessed at from the name. `Fhir_Bundle_Transaction_Post_Returns_NotFound_Unsupported_By_This_Version` in `PatientApiTests.cs` asserts the real `404`.

### Cross-cutting API concerns
- [x] OAuth2: no bearer token rejected, malformed bearer token rejected, refresh token flow (confirmed requires the `offline_access` scope at registration/token-request time — omitted by default in this project's main test scope) issues a new working access token. True token-**expiry** rejection is not covered: access tokens are 1-hour JWTs signed by the server, so simulating real expiry would require either waiting out the full TTL or forging a JWT with the server's private key — neither is practical for this suite, so this is a documented, deliberate gap rather than a silent one (same rationale as the FHIR 501 deferral)
- [x] Pagination on list endpoints — confirmed `_offset`/`_limit` genuinely page through `GET /api/patient` (non-overlapping ids across pages), not just accepted-and-ignored
- [x] Rate limiting / throttling — confirmed **not enabled** on this instance (no rate/throttle-related row in the `globals` table); no test written, per the "confirm first, don't build against an assumption" guidance
- [x] Malformed JSON body returns 400, not 500 (confirmed on `POST /api/facility` with a truncated body — falls through to the normal missing-required-field validation path, not a crash)
- [x] RBAC: a token registered and granted only `user/patient.read` can call `GET /api/patient` but gets `401` on `GET /api/facility`, confirmed via `_rest_config.php`'s `scope_check()` (`user/{resource}.{permission}` string compared against the token's granted scopes, hard `401` on a miss) rather than assumed

## Layer 2 — Database (C#/xUnit)

- [x] `patient_data`: seeded demo rows present, `pid` uniqueness, required-field integrity
- [x] `openemr_postcalendar_events`: no orphaned `pc_pid` references
- [x] Transactional insert/verify/rollback pattern proven
- [x] `form_encounter`: every encounter row has a valid `pid` and `provider_id` (confirmed no DB-level FK exists on either column, same application-only-enforced pattern already seen on insurance/billing; `provider_id = 0` means unassigned and is correctly excluded from the orphan check, not treated as a dangling reference)
- [x] `users`: no duplicate populated usernames (confirmed no DB-level unique constraint on `username` — duplicates are only prevented, if at all, at the application layer), disabled users excluded by the exact `active = 1` predicate `AuthUtils.php`'s login check uses (confirmed via source)
- [x] Reference code lists for allergy/immunization — wording corrected: the real immunization vaccine reference set turned out to live in `codes`/`code_types` (the standard external-code-set mechanism, filtered to `ct_key = 'CVX'`), not in `list_options` as the original wording assumed; confirmed every stored `immunizations.cvx_code` resolves to a real CVX entry there. Allergy's reference set (`list_options` where `list_id = 'allergy_issue_list'`) is confirmed populated. This is the DB-layer version of the check — literally comparing against rendered UI dropdown HTML is a distinct, still-open UI-layer task, not something a DB test can verify
- [x] Soft-delete: only a `deleted` flag exists in this schema (`documents`, `pnotes`, `forms`, `onsite_mail`, `ar_activity`, `ccda_table_mapping`) — there is no `date_deleted` column anywhere; confirmed a soft-deleted `documents` row is excluded by the exact predicate `DocumentService::getAllAtPath()` uses (`deleted = 0`)
- [x] Audit/log table (`log`) gets a row on application-mediated patient creates — confirmed, and the cross-project race against `OpenEmr.Api.Tests` on a fresh, zero-history container (found via the Session 1 fresh-container check) is now resolved: `AuditLogSeedFixture` gives `OpenEmr.Db.Tests` its own self-contained OAuth2 client + real patient-create call, so the assertion no longer depends on `OpenEmr.Api.Tests`'s timing at all (see `CONTEXT.md` Known Constraints). Confirmed **zero DB triggers exist anywhere in this schema** — audit logging is entirely PHP-mediated (`EventAuditLogger::newEvent()`), so a direct SQL write bypassing the app leaves no audit trail at all (HIPAA-relevant gap, not a test artifact)
- [x] Referential integrity across insurance ↔ patient, billing ↔ encounter — confirmed **neither relationship has a DB-level FK constraint** (both tables are InnoDB, which supports FKs, but none are declared); a direct insert with a nonexistent `pid`/`encounter` is silently accepted by the schema and only catchable via an application-style orphan-detection query, not a DB-enforced guarantee

## Layer 3 — UI (Playwright/TypeScript)

- [x] Auth: valid login, invalid password, empty password, logout — verified against live instance
- [x] Scheduling: create appointment shows on calendar — verified against live instance
- [x] Scheduling: double-booking same provider slot — verified as a **soft warning** (`confirm("Provider not available, use it anyway?")`), not a hard block; the checkbox label above undersold this until it was actually run. See `HANDOFF.md` for the still-flaky test and open follow-up.
- [x] Scheduling: cancel appointment — root-caused and fixed. The real bug was OpenEMR's own day-view single-click-to-edit handler throwing a JS error for "No Show"-category appointments (see `FINDINGS.md` #14), compounded by two test bugs (no patient-scoping on the day-view element lookup, no wait for the edit dialog's iframe to load). `CalendarPage` now double-clicks the appointment element (bypassing the broken single-click handler) and scopes the lookup to the patient's name; confirmed via a direct DB check that the row is actually removed, not just visually gone.
- [x] Scheduling: reschedule — confirmed working via the classic UI edit form (change date/time on an existing appointment and save triggers a real `UPDATE`, a genuinely different code path from `add_edit_event.php`'s own insert/update block than the REST API's `AppointmentService::insert()`). **Drag-and-drop rescheduling does not exist in this OpenEMR version's calendar day/week view at all** — confirmed by reading source: `interact.js` is loaded for the calendar page, but its `.drag-action`/`.resize-action`/`.draggable`/`.droppable` wiring (in `library/js/utility.js`) is applied to unrelated features (the telehealth conference room window, the help modal) — a repo-wide search of `interface/main/calendar/` for those classes returns zero matches. See `CONTEXT.md`.
- [x] Scheduling: recurring appointment series — confirmed working via the classic UI (`#form_repeat` + `form_repeat_freq`/`form_repeat_type`/`form_enddate`), unlike the REST API which silently ignores recurrence fields entirely (see the Appointment API line above). A weekly-recurring appointment created today is confirmed to also render on the following week's day view — the calendar expands the single stored `pc_recurrtype`/`pc_recurrspec` row dynamically per viewed date rather than pre-creating N rows.
- [x] Scheduling: provider-availability filtering — the `#pc_username` multi-select filter mechanism itself works, but **this environment only has one real calendar-visible provider (`admin`)**, so a genuine "provider A's appointments disappear when B is exclusively selected" test isn't possible here. What's confirmed and tested instead: clearing the filter entirely (deselecting every option) doesn't show an empty calendar — the browser submits no `pc_username[]` field at all when nothing is selected (standard `<select multiple>` behavior), and the server falls back to the current session user's own schedule rather than rendering nothing. See `CONTEXT.md`.
- [x] Patient registration: new patient intake form (happy path), required-field validation — verified against live instance
- [x] Patient registration: duplicate patient detection — verified; OpenEMR's own new-patient flow always runs a name+DOB match check and shows a review popup (even on zero matches), covered by `patient-registration.spec.ts`
- [x] Clinical encounter: open encounter, add a SOAP note, sign/lock — `clinical-encounter.spec.ts`. Creates a patient + visit (which the app itself treats as the encounter), opens the SOAP form via the Clinical dropdown in the encounter's forms panel, fills all four SOAP fields and saves, then eSigns it. Confirmed live: eSigning a form is what sets it to "Locked" — there's no separate lock checkbox in this flow (the `#lock` checkbox in the eSign modal's markup exists in source but didn't render here, per-form eSign and lock appear to be the same action for this form type). See `CONTEXT.md`.
- [x] Billing: apply a payment — verified against live instance (`billing-payment.spec.ts`); creates a patient, opens a visit via Patient > Visits > Create Visit, then posts a cash payment via Fees > Payment and asserts the resulting receipt. Claim generation is still open, not covered by this test.
- [x] Billing: generate a claim — `billing-claim.spec.ts`. Creates a patient + visit, adds an established-patient CPT 99213 code via the Fee Sheet, then generates a CMS 1500 PDF claim from Billing Manager and asserts a real PDF download occurs. See `CONTEXT.md` for the multi-step navigation this required (the "Insurance" link on the Billing Manager grid is a red herring — it navigates to the patient's insurance-info edit page, not claim generation at all).
- [x] RBAC in the UI: a front-desk role can't see clinical notes, a provider role can't access admin settings — `rbac.spec.ts`. Both a fresh "Front Office" user and a fresh "Clinicians" user are confirmed to have no "Admin" top-nav menu item at all (matching the requested "provider can't access admin settings"). For "front-desk can't see clinical notes," the cleanest, most decisive signal turned out to be one level upstream of the SOAP note itself: a front-desk user's own "Create Visit" form only offers the unselectable placeholder option in the Visit Category dropdown (confirmed: exactly 1 option vs. 7 real categories for a Clinicians-group user) — they can't create a real clinical encounter at all, so the SOAP note screen is unreachable by construction, not just hidden. See `CONTEXT.md` for the full Add User form exploration.
- [x] Admin duplicate-patient management: two patients with matching name/DOB/sex are flagged, and can be merged — `admin-duplicate-merge.spec.ts`. Creates two identical fixture patients (same fname/lname/DOB/sex — this alone scores 20 on the app's own duplicate-scoring formula, well over the `dupscore > 12` display threshold), navigates Admin > Patients > Manage Duplicates, selects "Merge and Discard" on the paired row, confirms via the Merge Patients screen, and asserts "Merge complete." Also confirms the specific merged pair's row (identified by the presence of the "Merge and Discard" option, not just by shared last name — this environment has enough accumulated fixture-data noise that unrelated patients can coincidentally share a `dupscore > 12` match, see `CONTEXT.md`) no longer appears afterward. See `CONTEXT.md` for the real duplicate-scoring formula and merge mechanics.
- [x] Patient portal: separate login flow, patient can view own appointments only — `patient-portal.spec.ts`. This OpenEMR version's patient portal was fully disabled/unconfigured before this session (`portal_onsite_two_enable=0`, plus a placeholder `portal_onsite_two_address`, `enforce_signin_email=1` requiring an extra trusted-email field, and forced credential reset on) — enabling it required real environment bootstrap (same narrow "confirm first, fix at the DB/env level" pattern as the FHIR and OAuth-client-enable fixes), not just new test code; see `CONTEXT.md` for the full write-up and `docker-compose.yml` for the corresponding `OPENEMR_SETTING_*` additions. The test creates a patient, enables "Allow Patient Portal" + a contact email via Demographics > Choices/Contact, generates real portal login credentials via the "Patient Portal / API Access" card's "Create" action, books an appointment for that patient via the classic calendar, then logs into the actual separate `/portal/index.php` login (a second browser tab, since it's a genuinely separate session/cookie from the clinician frameset) with the generated credentials and confirms exactly one appointment renders — proof of both real portal auth and patient-scoped data access (the portal's own appointment query is hard-scoped to the session's `pid` server-side, confirmed by reading `portal/home.php`).

## Layer 4 — Grey-area reliability testing (race conditions, concurrency)

Per `CONTEXT.md`'s Purpose & Vision item 3, this phase was deliberately deferred until the straightforward coverage above was solid. It's now started, scoped narrowly to a small number of concretely-confirmed races rather than an attempt to enumerate every possible race across the whole API surface — `ConcurrencyApiTests.cs`/`ConcurrencyDbTests.cs`'s `Task.WhenAll`-firing pattern is reusable for extending this list in a future session.

- [x] Concurrent identical `POST /api/patient` requests racing on the unguarded `MAX(pid)+1` computation — confirmed a real, previously-undocumented defect: most concurrent creates silently fail while reporting `200` (`FINDINGS.md` #18). A companion DB-layer test independently confirms the DB's own unique index on `pid` genuinely does prevent two readable rows from ever sharing a pid — the app's *handling* of the resulting collision is what's broken, not the absence of a DB-level guard.
- [x] Concurrent `PUT`s to the same Message id — confirmed a genuine lost-update race under real load (only 6–7 of 10 concurrent marker-appends survive), extending the already-known `FINDINGS.md` #1/#3 missing-locking pattern from a sequential-wrong-id demonstration to real concurrent traffic (`FINDINGS.md` #19).
- [x] Concurrent `DELETE`s of the same Appointment id — confirmed all report success regardless of which single request actually performed the delete (`FINDINGS.md` #19).
- [x] Concurrent `PUT`s to the same Patient Insurance record, each touching a different field — confirmed a real, concurrency-amplified escalation of the already-known destructive-overwrite defect (`FINDINGS.md` #4): of 5 concurrent single-field updates, exactly one survives (whichever commits last), silently erasing the other four callers' already-applied changes with no error to anyone (`FINDINGS.md` #20).
- [x] Concurrent Practitioner creates sharing an identical username — confirmed a genuinely unguarded race, not just a narrow timing window: `users.username` has no uniqueness check at either the DB or application layer, so every one of 5 concurrent `POST /api/practitioner` requests with the same username succeeds, every time (`FINDINGS.md` #21). A different flavor from the other three: nothing here ever fails, which is arguably a starker gap than a guard that only sometimes loses a race.

## Gaps outside the three core layers (worth adding once the above is solid)

- [x] **Accessibility** — axe-core scan on login, calendar, and patient registration pages, via `@axe-core/playwright` in `accessibility.spec.ts`. Gates on `critical`-impact violations only (the `treeLine`-crawl baseline's 607 violations are almost entirely `serious`/`moderate` color-contrast/landmark issues, not something a first pass should try to zero out). Found two new real, confirmed `critical` violations neither in the treeLine baseline nor previously known (that crawl never reached login or patient registration) — see `FINDINGS.md` #15 (login page's language `<select>` has no accessible name) and #16 (patient registration's collapsible-section buttons have invalid `aria-controls` values). Calendar's default landing view (via the top-nav, not the specific day-view URL treeLine crawled) has zero critical violations — it also doesn't render the `#pc_username` provider-filter select that the treeLine-crawled day-view URL flagged as `critical`, a different view of the same page rather than a contradiction. Both known findings are asserted directly by exact rule id (matching this project's established pattern of documenting real, current behavior rather than a should-be-clean ideal — see `FINDINGS.md` #1), so the tests stay green until either defect is actually fixed or a genuinely new regression appears.
- [x] **Load/performance** — `load/appointment-booking.js` (k6) creates one fixture patient, then fires 20 concurrent virtual users at `POST /api/patient/{pid}/appointment` for the identical date/time/provider slot, asserting (via a `teardown()` read-back through the same list endpoint used elsewhere in this project) that every request produced its own distinct `pc_eid` — no lost writes, no id collisions under real contention — plus standard `http_req_duration`/`http_req_failed` thresholds. `load/run-load-test.mjs` is the orchestrator: registers + DB-enables a dedicated OAuth2 client (same narrow, documented bootstrap exception as `OAuthTokenFixture`, here done via `docker compose exec mariadb mariadb -e ...` since this MariaDB 11.4 image ships `mariadb`, not `mysql`, as the CLI binary name) and requests a password-grant token before invoking `k6 run` with it. Run via `node load/run-load-test.mjs` against a running stack. Confirmed this reinforces, not contradicts, the existing double-booking finding — see `CONTEXT.md`.
- [x] **Contract/schema validation** — `FhirSchemaValidator` (`tests/OpenEmr.Api.Tests/Fhir/FhirSchemaValidator.cs`) validates real responses against the official, vendored `fhir.schema.json` (HL7's own R4 JSON schema, `tests/OpenEmr.Api.Tests/Fhir/Schemas/fhir.schema.json`) via `NJsonSchema`, replacing the old bare `resourceType == "Bundle"` spot-check across all 7 FHIR resources this project covers (Patient, Appointment, Encounter, AllergyIntolerance, Condition, MedicationRequest, Observation). This immediately found a real, systemic, previously-undetected defect — see `FINDINGS.md` #17: every Bundle's own `meta.lastUpdated` is missing its required timezone offset, failing the FHIR `instant` pattern, though the resources nested inside each Bundle are individually schema-compliant. The 4 tests that already isolate a specific fixture-created entry (Encounter, AllergyIntolerance, Condition, MedicationRequest) go one step further and validate that entry's own resource against its specific resource-type schema definition too, not just the surrounding Bundle envelope.
- [x] **Security/negative** — three confirmed-safe results, none of them assumed: (1) `Get_Patient_List_With_Sql_Injection_Shaped_Lname_Search_Is_Treated_As_A_Literal_String_And_Leaves_Data_Intact` (`CrossCuttingApiTests.cs`) fires 4 SQLi-shaped payloads (`' OR '1'='1`, a `DROP TABLE`, a `UNION SELECT` against `users`, an inline-comment bypass) at `GET /api/patient?lname=...` — all return `200` with zero matches (treated as an ordinary non-matching string by the parameterized search builder, not a crashed query or a bypassed filter), and the table is confirmed still queryable and intact immediately after each attempt; (2) `security.spec.ts`'s XSS test creates a patient whose last name is `<img src=x onerror="window.__xssFired=true">XSS...` and confirms on the real demographics page that the marker text renders, `window.__xssFired` never got set, and the raw HTML has the tag HTML-escaped (`&lt;img`), not literal; (3) `security.spec.ts`'s session-fixation test captures the `OpenEMR` session cookie before and after login and confirms the value is regenerated on authentication, not carried over from the pre-login session. See `CONTEXT.md`'s Known Constraints for the full write-up — none of these needed a `FINDINGS.md` entry since nothing broke.
- [x] **Test data lifecycle** — confirmed CI was never actually exposed to this gap (`run-suite.yml` already does a fresh `docker compose up -d` per run and `down -v` on teardown), so the accumulation was purely a local-dev-environment problem: a 5-day-old local container had grown to 956 `patient_data` rows and 874 `insurance_data` rows with no cleanup, which surfaced as a genuine failing test (`ReferentialIntegrityDbTests.Insurance_Data_Does_Not_Reference_Orphaned_Patient_Ids`, 54 real orphaned `insurance_data` rows referencing patients no longer in `patient_data`) rather than a hypothetical risk. Fixed by adding `docker/reset-env.mjs`, a small Node orchestrator (same style as `load/run-load-test.mjs`) that runs `docker compose down -v && up -d` against a fresh volume and polls both containers past the normal early `unhealthy` readings until genuinely healthy. Confirmed the reset requires no manual browser follow-up: `LoginPage.loginAs()` already auto-dismisses the product-registration modal (`dismissRegistrationModalIfPresent()`), and `docker-compose.yml`'s `OPENEMR_SETTING_*` env vars already cover REST/FHIR/OAuth-password-grant/portal — the "first-login one-time steps" in `HANDOFF.md` turned out to only matter for manual interactive exploration, not for the automated suite, which is exactly why CI never needed them either. Also confirmed `DEMO_MODE` really doesn't reseed anything on this pinned image (0 rows in `patient_data` immediately after a reset, consistent with the existing `CONTEXT.md` constraint) — the DB test project's own fixture-seeding is what the suite actually depends on, not demo data. Run via `node docker/reset-env.mjs`. A full `dotnet test OpenEmr.Tests.sln` against the freshly reset environment passed clean (121/121), including the previously-failing orphan check. `Immunization`/`Procedure`/`Prescription`'s direct-SQL fixture inserts (and every API test class's patient creation, project-wide) still leak permanently between runs with no per-test cleanup — that's unchanged and considered acceptable, since a full periodic reset via this script is the intended reset mechanism, not per-test teardown. The one still-deliberate exception is `ProcedureApiTests`'s intentionally-orphaned `procedure_order` row, cleaned up in a `finally` block because leaving it behind would poison every other Procedure list-based test with a `500` — a correctness requirement, not general hygiene. `ReferentialIntegrityDbTests`' and `FormEncounterDbTests`' insert/rollback pairs remain the one place in the suite that already leaves zero footprint by construction.

## Suggested build order

1. Finish Layer 1 (API) resource-by-resource — it's the fastest layer to iterate on and de-risks the OAuth2/demo-data assumptions the other two layers depend on.
2. Layer 2 (DB) alongside it, since most DB tests are "verify what the API just wrote."
3. Layer 3 (UI) last, once you know appointment/patient IDs and demo credentials are stable — UI selectors are the most likely thing to need adjustment against the real running container, so validate them with `npx playwright codegen` before writing more UI specs.
